# 내 풀이
def solution(a, b):
    num_list = [a, b]
    b = max(num_list)
    a = min(num_list)
    answer = 0
    for num in range(a, b+1) :
        answer += num
    return answer

# 좋아보이는 풀이
def adder(a, b):
    if a > b:
        a, b = b, a
    return sum(range(a, b + 1))
